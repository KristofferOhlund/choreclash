from db import db
from models import user

class UserService():
    """Class for handling Users"""
    def register_user(self, username, password):
        ...

    def login_user(self, username, password):
        # helpers.validateUser(username, password)
        # return User(username, password)
        ...