[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "choreclash"
version = "0.1.0"
description = "ChoreClash"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
